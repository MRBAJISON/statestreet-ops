import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // This app stores uploaded org logos and generated QR images as runtime data
      // URLs, so Next Image optimization is not useful for those surfaces.
      "@next/next/no-img-element": "off",
      // Client-side fetch hooks and route-synchronized drawer state are intentional
      // in this app. Keep derived-state guidance in AGENTS/docs instead.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
